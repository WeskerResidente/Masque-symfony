{% extends 'base.html.twig' %}

{% block title %}Modifier un masque{% endblock %}

{% block body %}
    <h1>Modifier le masque : {{ masque.nom }}</h1>

    {% for message in app.flashes('success') %}
        <div class="alert alert-success">{{ message }}</div>
    {% endfor %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Mettre à jour</button>
    {{ form_end(form) }}
{% endblock %}
