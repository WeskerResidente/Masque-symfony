<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}

        <link rel="stylesheet" href="{{ asset('style/style.css') }}">
    </head>
    <body>
        {% block content %}
            <nav class="navbar">
                <div class="logo">
                    <a href="{{ path('app_home') }}">
                        <img src="{{ asset('images/logo.png') }}" alt="Logo Le Masque du Virtuose">
                    </a>
                </div>

                <div class="title-center">
                    <span>Le Masque<br>du Virtuose</span>
                </div>

                <ul class="nav-links">
                    <li><a href="{{ path('app_home') }}">Accueil</a></li>
                    <li><a href="{{ path('app_liste_masques') }}">Masques</a></li>
                    {% if app.user %}
                        <li><a href="{{ path('app_add_masque_controlleur') }}">Ajouter</a></li>
                        {% if is_granted('ROLE_ADMIN') %}
                            <li><a href="{{ path('admin') }}">Admin</a></li>
                        {% endif %}
                        {% if app.user %}
                            <li><a href="{{ path('app_mes_masques') }}"> Mes masques</a></li>
                        {% endif %}
                        <li><a href="{{ path('app_contact') }}">Contact</a></li>
                        <li>
                            <form action="{{ path('app_logout') }}" method="post">
                                <button class="btn-logout" type="submit">🚪 Déconnexion</button>
                            </form>
                        </li>
                    {% else %}
                        <li><a href="{{ path('app_register') }}">Inscription</a></li>
                        <li><a href="{{ path('app_login') }}">Connexion</a></li>
                    {% endif %}
                </ul>
            </nav>
        {% endblock %}
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                    <div class="global-flash {{ label }}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}
        {% block body %}{% endblock %}

</html>
